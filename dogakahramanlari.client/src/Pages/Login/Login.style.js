import styled from '@emotion/styled';

export const BackgroundWrapper = styled.div`
    .background {
        width:100vw;
        height: 100vh;
        &.blur {
            filter: blur(4px)
        }
    }
`;